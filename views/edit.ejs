<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/styleEdit.css">
</head>
<body>
    <%- include('partials/flash') %>
    <h1>Edit Post</h1>
    
    <% if (!post) { %>
        <p>Post not found.</p>
    <% } else { %>
        <p>Post by: @<%= post.author ? post.author.username : 'Unknown' %></p>
        
        <p>Post ID: <%= post._id %></p>
        Edit your tweet:
        <br><br>
        <form method="post" action="/tweets/<%= post._id %>">
            <textarea rows="10" cols="35" name="content"><%= post.content %></textarea><br>
            <button type="submit">Update Post</button>
        </form>
        <br>
        <a href="/tweets">Back to feed</a>
    <% } %>
</body>
</html>